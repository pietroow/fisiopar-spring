version: '3'

services:
  postgres:
    image: postgres
    volumes:
    - ./volumes/database:/var/lib/postgresql/data
    ports:
    - 5432:5432
    environment:
      POSTGRES_USER: root
      POSTGRES_PASSWORD: rootpass
      POSTGRES_DB: fisiopar